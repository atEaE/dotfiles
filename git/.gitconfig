[core]
	excludesfile = ~/.gitignore_global
	editor = code --wait

[alias]
	sw = checkout
	st = status
	cm = commit
	fp = fetch --prune
	rb = rebase
	pf = --force-with-lease
	tree = log --graph --pretty=format:'%x09%C(auto) %h %x09%C(auto)%s %d'
